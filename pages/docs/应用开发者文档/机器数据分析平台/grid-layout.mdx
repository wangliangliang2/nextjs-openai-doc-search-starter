
`<grid-layout>`元素定义整个仪表盘容器。

### 父元素

* `<dashboard>`

### 属性

|属性|说明|类型|默认值|
|:--:|:--:|:--:|
|gap-x|水平方向每个layout的间距|number|10|
|gap-y|垂直方向每个layout的间距|number|10|
|cols|水平方向的栅格数|number|48|
|row-height|垂直方向每个栅格的高度|number|30|

>注意：`<grid-layout>`组件的子元素请尽量都包裹一层`<layout>`