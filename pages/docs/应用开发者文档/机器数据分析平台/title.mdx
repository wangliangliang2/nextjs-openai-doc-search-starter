`<title>`元素用于定义图表的标题。

### 父元素

您可以将`<title>`元素放入以下父元素中。

* `<chart>`

### 子元素

|子元素|说明|默认值|
|:--:|:--:|:--:|
|`<name>`|标题名称|无|
|`<color>`|标题颜色|'inherit'|
	
示例：
```
<title>
  <name>实时监控数据</name>
  <color>red</color>
</title>
```	