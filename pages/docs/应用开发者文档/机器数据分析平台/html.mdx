
`<html>`元素用来渲染标准 html 元素，可以直接在标签里写入html元素，或者通过src属性来引入其他地址的 html 元素。
	
* 直接写html元素

 ```
<html>展示内容</html>
```

* 引入当前 app 下的 html 文件

 ```
<html src="<file_name>.html">
</html>
```

* 引入其他 app 下的 html 文件
```
<html src="<other_app_name>:<file_name>.html">
</html>
```