

`<operation>`元素定义图表的操作栏配置。

### 父元素

您可以将`<operation>`元素放入以下父元素中。

* `<chart>`

### 子元素

|子元素|说明|默认值|
|:--:|:--:|:--:|
|`<hide>`|是否在非编辑模式下隐藏图表操作栏|无|
	
示例：
```
<operation>
  <hide>true</hide>
</operation>
```	
