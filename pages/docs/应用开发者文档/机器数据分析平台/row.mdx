`<row>`元素定义数据表的行显示，根据行的数据值设置对应行背景色。

### 父元素

在`<chart>`元素下，只有当 type 属性指定如下值时，可以使用`<row>`元素。

* `<chart type="grid-table>`
* `<type="tree-table">`

### 子元素

|元素|说明|默认值|
|:--:|:--:|:--:|
|`<value>`|对应的需要值，可以是多个值|无|
|`<color>`|颜色设置|无|
