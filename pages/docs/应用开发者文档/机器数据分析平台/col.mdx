`<col>`为水平方向上的列布局。

### 父元素

* `<row>`

### 属性

|属性|描述|默认值|
|:--:|:--:|:--:|
|offset|栅格左侧的间隔格数，间隔内不可以有栅格|0|
|order|栅格顺序，flex 布局模式下生效|0|
|pull|栅格向左移动格数|0|
|push|栅格向右移动格数|0|
|span|栅格占位格数，span=0 时相当于 display: none|-|