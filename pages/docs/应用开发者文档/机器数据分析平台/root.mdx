`<root>`作为所有 xml 元素的根元素。**所有的视图 xml 文件都应该被包裹在`<root>`元素下**。`<root>`元素本身没有任何视图以及其它副作用，仅仅作为一个包裹容器。

示例：

```xml
<root>
  <dashboard>...</dashboard>
</root>
```

```xml
<root>
  <chart>...</chart>
</root>
```
