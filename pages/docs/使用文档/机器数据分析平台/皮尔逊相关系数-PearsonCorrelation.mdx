用 pearson 计算皮尔逊相关系数和对应的 p 值。皮尔逊相关系数用来描述两组数据的线性关系，在[-1,1]之间，绝对数值越大，两组数据的相关性越强，正数表示正相关，负数表示负相关，0 表示没有相关性。

**语法：**

```
...|score pearsonr <a_field> against <b_field>
```

**使用场景：**

- 只支持 1-1 的比较。
- 不支持模糊搜索（ \*）。
- 不接受任何参数。