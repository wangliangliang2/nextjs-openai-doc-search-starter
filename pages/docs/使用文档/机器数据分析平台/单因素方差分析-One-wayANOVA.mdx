用来检测提供的数据之间是否存在显著性差别。

**语法：**

```
...| score f_oneway alpha=<float> <field_1> <field_2> ... <field_n>
```

**参数说明：**

- alpha，用来指定显著性水平，即在零假设为真时拒绝零假设的概率，默认是 0.05。

**使用场景：**

- 支持模糊搜索（ \*）。
- 单因素方差分析的 H0 假设：样本之间不存在显著性差异；H1 假设：样本之间存在显著性差异