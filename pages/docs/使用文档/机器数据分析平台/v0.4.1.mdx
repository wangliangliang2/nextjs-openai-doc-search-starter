新功能：

* SPL 新增支持 [mvexpand](/express/manual/6495/mvexpand)、[mvcombine](/express/manual/6496/mvcombine) 算子。

* Pandora 2.0 安装包支持下载验证文件。


功能优化：

* 数据分析结果导出功能交互优化。

* 日志过长情况下的搜索结果展示页面卡顿优化。

* SPL 语法错误检查和语法提示。




Bug 修复：

* 搜索结果显示数据条数但未显示实际数据。

* 审计日志规范化：login请求未记录用户名、未记录处理请求的具体host。

* 搜索分析页面输入spl带换行导致执行失败。

* 数据表图表类型修改搜索条件后列的样式被重置。

* xml类型字段提取方式解析失败。

* 仪表盘中用交互式方式创建的图表里，搜索条件的模板变量不生效。

* 编辑app里仪表盘的搜索条件时，模板变量被模板变量的当前值取代。

* App 通过编辑xml关闭仪表盘刷新频率无法保存。

* 仪表盘导出xml文件里模板变量语法问题。

* 仪表盘字段内容类型的模板变量获取字段内容不完整。