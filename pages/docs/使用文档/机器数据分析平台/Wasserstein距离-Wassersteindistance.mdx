用来计算两个样本之前的 Wasserstein 距离。

**语法：**

```
...|score wasserstein_distance <a_field> against <b_field>
```

**使用场景：**

- 只支持 1-1 的比较。
- 不支持模糊搜索（ \*）。
- 不接受任何参数。
- Wasserstein 距离的 H0 假设：输入的两个字段符合概率分布；H1 假设：输入的两个字段不符合概率分布。