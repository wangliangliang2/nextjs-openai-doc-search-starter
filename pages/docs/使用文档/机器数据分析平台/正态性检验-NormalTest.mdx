用来检测样本是否符合正态分布。

**语法：**

```
...| score normaltest alpha=<float> <field_1> <field_2> ... <field_n>
```

**参数说明：**

- alpha，用来指定显著性水平，即在零假设为真时拒绝零假设的概率，默认是 0.05。

**使用场景：**

- 支持模糊搜索（ \*）。
- 正态性检验的 H0 假设：样本符合正态分布；H1 假设：样本不符合正态分布
