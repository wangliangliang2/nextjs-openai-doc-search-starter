用 describe 计算数据中的多项统计指标：样本量、最大值、最小值、平均值、方差、偏度、峰度。

**语法：**

```
...|score describe ddof=<int> bias=<bool> <a_field_1> ... <a_field_n>
```

**参数说明：**

- ddof，用来指定自由度（只用于方差计算）。如不提供，默认为 1。
- bias，用来指定在计算偏度和峰度时，是否考虑统计误差。如不提供，默认为 False。