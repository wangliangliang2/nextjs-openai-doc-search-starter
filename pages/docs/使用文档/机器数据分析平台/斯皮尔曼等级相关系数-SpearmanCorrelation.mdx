斯皮尔曼相关系数被定义成等级变量之间的皮尔逊相关系数。它利用单调方程评价两个统计变量的相关性。若数据中没有重复值，且当两变量完全单调相关时，斯皮尔曼相关系数为 +1 或 −1 。

**语法：**

```
...|score spearsonr <a_field> against <b_field>
```

**使用场景：**

- 只支持 1-1 的比较。
- 不支持模糊搜索（ \*）。
- 不接受任何参数。
