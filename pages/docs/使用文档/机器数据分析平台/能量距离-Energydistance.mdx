用energy_distance算子来计算两个一维分布之间的能量距离。能量距离是一个统计距离之间的概率分布。

**语法：**

```
...| score energy_distance <a_field> against <b_field>
```

**使用场景：**

- 只支持 1-1 的比较。
- 不支持模糊搜索（ \*）。
- 不接受任何参数。
- 能量距离的 H0 假设：输入的两个字段符合概率分布；H1 假设：输入的两个字段不符合概率分布。
