双样本 KS 检验用来检测两个独立样本是否来源于同一已知分布，即检验两个数据分布是否一致。

**语法：**

```
...| score ks_2samp alpha=<float> <a_field> against <b_field>
```

**参数说明：**

- alpha，用来指定显著性水平，即在零假设为真时拒绝零假设的概率，默认是 0.05。

**使用场景：**

- 只支持 1-1 的比较。
- 不支持模糊搜索（ \*）。
- 双样本 KS 检验的的 H0 假设：两个数据分布一致；H1：两个数据分布不一致