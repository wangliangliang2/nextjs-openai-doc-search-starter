rare 命令统计数据中出现频率最低的值。同时也可以用 by 字句指定多个分组字段，统计每个分组中的罕见值。

**语法**

```
rare <N field> [by field][, fieldn ...]
```

**参数说明**

必要参数：

* `field`：被统计的字段。

* `N`：统计字段频率最低的前N个值。

可选参数：

* `by field`：指定分组字段名称。

**用法示例**

返回每个主机中出现频率最低的前5个url。

```
……| rare 5 url by host
```
