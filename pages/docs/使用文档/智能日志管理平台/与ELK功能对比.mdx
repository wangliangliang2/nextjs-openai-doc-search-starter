与开源 ELK 日志分析系统相比，七牛云智能日志分析平台重点增强了查询分析与可视化功能，支持使用专业日志采集工具 logkit-pro 采集上百种数据源的日志数据、支持对实时日志数据输出、支持配置报表对日志分析结果可视化等，并在稳定性、查询性能、计算数据量方面做了深度优化。

# **产品免费开通使用**

免费试用额度：新增日志数据 1 GB/月，存量日志数据 1 GB/月，日志仓库 1 个/月，API 调用次数 100 万次/月， 点此[了解详情](/insight/manual/4676/free-allowances)。

产品开通相关：在使用智能日志管理平台之前，您需要先开通智能日志平台服务。详情请阅读[产品开通](/insight/manual/4605/opening-the-product)。

具体对比分析如下：

# **易用性对比**

|功能|功能子项|开源 ELK|七牛智能日志管理平台|
|:-------:|:--------:|:-------:|:-------:|
|<br/><br/><br/><br/><br/><br/>日志采集|<p><br/>数据源<br/><p>配置<br/><p>数据流<br/><p>内存占用<br/><p>CPU 占用<br/><p>集群管理<br/><p>agent 监控<br/><p>定制化开发<br/><p>|<p><br/>通过 Logstash/Beats/FluentD 采集，依赖社区<br/><p>复杂的文件配置<br/><p>结合 Logstash/Beats/FluentD 才能完成数据解析、转换<br/><p>内存至少占用 500～600M<br/><p>CPU 至少 2～3 核且不可控<br/><p>不支持<br/><p>不支持<br/><p>不支持|<p><br/>logkit-pro 独立采集上百种数据源<br/><p>极其易用的可视化配置<br/><p>通过可视化操作完成复杂的数据解析与转换<br/><p>内存占用在 100M 以内<br/><p>CPU 可按需调控<br/><p>支持<br/><p>支持监控 agent 运行状态<br/><p>支持|
|<br/><p>数据源配置|<br/><p>单元|<br/><p>提供 Index 概念用以区分不同日志，用户手动配置|<br/><p>服务端统一配置，无需用户操作|
|<br/><br/><br/>扩容|<br/><p>存储<br/><p>机器<br/><p>配置<br/><p>采集点|<br/><p>增加机器，购买云盘<br/><p>新增机器<br/><p>配置 Logstash 并通过配管系统应用机器<br/><p>通过配管系统控制，将配置和 Logstash 安装到机器组|<br/><br/><br/>自动扩容|
	
更多 logkit-pro与主流采集工具对比请阅读 [logkit-pro 与主流收集组件的功能性能对比](/insight/manual/4934/logkit-pro-compared-with-mainstream-collecting-component-function-performance)。
	
# **查询分析功能对比**

|功能|功能子项|开源 ELK|七牛智能日志管理平台|
|:-------:|:-------:|:-------:|:-------:|
|<br/><br/><br/><br/><br/><br/><br/><br/>日志查询|<p>索引查询<p>字段分词<p>划词过滤<p>模糊搜索<p>日志排序<p>上下文查询<p>上下文过滤<p>SPL 支持<p>JSON 查询<p>Geo 查询<p>IP 查询<p>查询多步返回<p>监控告警<p>|<p>支持<p>支持<p>不支持<p>支持<p>支持<p>支持<p>不支持<p>不支持<p>支持<p>支持<p>支持<p>不支持<p>不支持|<p>支持<p>支持<p>支持<p>支持<p>支持<p>支持<p>支持<p>支持<p>支持<p>支持<p>支持<p>支持<p>支持|
|<br/><p>日志聚合|<p>分桶<p>指标分析<p>快速统计|<p>支持<p>支持<p>支持|<p>支持<p>支持<p>支持|
|<br/><p>数据投递|<br/><p>实时投递<br/><p>离线投递<br/><p>计算投递|<br/><p>支持，API/SDK<br/><p>支持，API/SDK<br/><p>支持，API/SDK|<br/><p>支持，API/SDK+工作流（直接导出到日志分析、对象存储等平台）<br/><p>支持，API/SDK+工作流（直接导出到日志分析、对象存储等平台）<br/><p>支持，API/SDK+工作流（直接导出到日志分析、对象存储等平台）|
|<br/>机器学习|<p>异常检测<p>预测<p>|<p>不支持<p>不支持|<p>支持<p>支持|
|<br/><br/><p>安全策略|<p>账户管理<p>权限管理<p>安全审计<p>多租户|<p>不支持<p>不支持<p>不支持<p>不支持|<p>支持，RBAC 访问控制<p>支持<p>支持<p>支持，https 和数据传输加密|

# **性能对比**
|功能|功能子项|开源 ELK|七牛智能日志管理平台|
|:-------:|:-------:|:-------:|:-------:|
|<br/><br/>稳定性|<br/><p>仓库断点续写<p>仓库写入缓存<p>日志搜索安全限制<br/><p>|<br/><p>不支持<p>不支持<p>不支持|<br/><p>支持<p>支持<p>支持|


# **数据规模对比**
|功能|功能子项|开源 ELK|七牛智能日志管理平台|
|:-------:|:-------:|:-------:|:-------:|
|日志采集|采集 Agent|写入 GB-TB/Day、存储在 TB 级|单日可以索引几百 TB 级数据，单次查询可以在秒级处理 10TB 规模数据，在数据规模上可以做到弹性伸缩与水平扩展|

总体而言，ELK 在安装和配置比较复杂。相比起来，七牛智能日志分析平台是一个一站式的日志采集、分析、可视化、计算、投递平台，不仅覆盖了 ELK 的功能，且以极低的学习成本让用户可以快速上手。并且过程中不需要担心容量、并发等问题，按量计费，弹性伸缩。

