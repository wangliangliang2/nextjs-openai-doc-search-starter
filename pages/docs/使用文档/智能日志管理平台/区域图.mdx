区域图是在折线图的基础之上形成的，它将折线图中折线与坐标轴之间的区域使用颜色进行填充，这个填充即为我们所说的面积，颜色的填充可以更好的突出趋势信息。和折线图一样，区域图也用于强调数量随时间而变化的程度，用于突出总值趋势。

**使用步骤**

1.在搜索栏输入搜索条件，点击右侧的查询按钮；

2.在`指标与分组`配置里配置好需要显示的分组字段与指标；

3.选择图表类型为区域图；

4.设置图表样式。

**区域图样式设置**

|配置项|说明|
|:-:|:-:|
|展示指标|筛选指标展示|
|空值模式|零：将空值的数据点转为零点；空值：将空值的数据点显示为间隙；连接：越过空值的数据点连接前后数据点|
|堆叠模式|对于有分类的数据字段可相邻显示各类的值，也可堆叠显示|
|对比基线|添加对比基线，对比值可以是常量值或者指标|
|图例|图例数值：可随图例展示指标值，可选当前值、平均值、最大值、最小值、总和</p>图例位置：图例在图表中的位置|
|x 轴|标签旋转:可设置显示方式，在 x 轴标签特别长的情况下可用来优化图表显示</p>标签显示：设置 x 轴标签显示风格</p>坐标轴名称：设置 x 轴名称|
|y 轴|指标刻度：使用对数指标单位刻度在轴值⾮常⼩或⾮常⼤时⾮常有⽤</p>最大值、最小值：y 轴显示的最大值、最小值</p>数值单位：支持完整的单位体系，对图表的单位进行切换</p>坐标轴名称：设置 y 轴名称|
|空值模式|将空值的数据点转为零点；将空值的数据点显示为间隙；越过空值的数据点连接前后数据点|
|对比分析|设置对比时间，对数据进行同比分析、环比分析和自定时间偏移分析|

一般区域图：所有的数据都从相同的零轴开始。

示例：

![](https://pandora-kibana.qiniu.com/easy_areachart1.png)

* 堆叠区域图：每一个数据集的起点不同，都是基于前一个数据集。用于显示每个数值所占大小随时间或类别变化的趋势线，堆叠起来的区域图在表现大数据的总量分量的变化情况时格外有用。

示例:比较 bytes_sent 在 bytes_sent 和 body_bytes_sent 总和中的占比情况

![](https://pandora-kibana.qiniu.com/report/area_chart_3.png)

![](https://pandora-kibana.qiniu.com/report/area_chart_2.png)