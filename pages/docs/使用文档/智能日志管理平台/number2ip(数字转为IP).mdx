number2ip transformer的功能就是 将整型数据/字符串转换为ip。

如整型数据为为 `223556667`，转换后变为 `13.83.52.59`.

计算方式如下：

```
223556667 = 13*256*256*256 + 83*256*256 + 52*256 + 59
```

而此transformer填入的是数字 `223556667`，我们根据按8位转换得到IP数据。

