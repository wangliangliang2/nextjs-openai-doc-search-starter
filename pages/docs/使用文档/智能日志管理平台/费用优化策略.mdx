在费用优化方面，我们给出以下建议，帮助您减少成本：
* 创建日志仓库时默认开启全文检索和全量索引，便于更多的字段被检索和聚合。

* 在实效性要求比较高的应用场景，可通过缩短存放在日志仓库里的时长，比如 7 天后自动导出到对象存储做低成本的永久存储。

* 合理设置 logkit-pro 日志上传参数，尽量使用 64KB~1MB 大包写入，减少数据推送 API 请求次数。

* 合理设置实时仓库数量，一般一个业务设置一个实时仓库即可。