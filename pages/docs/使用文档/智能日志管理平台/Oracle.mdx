### **基础配置信息**
  * 数据库连接字符串(`DSN`):用户名/用户密码@oracle地址:端口/服务名，如:sys/password@127.0.0.1:1521/xe?as=sysdba。 `！注意：请使用内网方式访问，以免出现安全隐患！`

  * 要插入的表名(`table_name`)：要插入的表名(仅支持单个表插入)，注意 oracle 中大小写敏感，请使用准确的表名。

  * 要忽略的字段名：填写要忽略插入的字段名表名，以逗号`,`分隔。