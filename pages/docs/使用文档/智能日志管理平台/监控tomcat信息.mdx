tomcat 监控主要用于监控 tomcat 状态。

tomcat 监控配置界面如下：
![](https://dn-odum9helk.qbox.me/FqbfkHgOrozHTMIDHG8GgiHioCRz)
需要填写的信息如下：
1. `url`: 默认为 http://127.0.0.1:8080/manager/status/all?XML=true。
2. `username`: 默认为 tomcat。
3. `password`: 默认为 s3cret。

如收集不到任何数据，请手动打开 url，并输入用户名密码，确保能正常访问 url