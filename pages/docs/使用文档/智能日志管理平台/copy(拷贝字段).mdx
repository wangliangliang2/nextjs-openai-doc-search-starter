copy转换器：拷贝数据的字段值到新字段中。
![](https://dn-odum9helk.qbox.me/FhR9c-61jypBimsC3avaJKOa3Nl1)

* 要进行Transform变化的键(`key`): 必填。支持多级嵌套，多级间用 *.* 符号连接，如 abc.xyz，表示 data["abc"]["xyz"]中的数据。

* 新的字段名(`new`): 必填，新的字段名称。