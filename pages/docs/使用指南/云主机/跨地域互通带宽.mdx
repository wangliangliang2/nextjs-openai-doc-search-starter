要实现跨地域网络实例互通，在购买带宽包后，您还需要设置跨地域互通带宽。

每个带宽包下的跨地域互通带宽的总和不能大于该带宽包的带宽值。云企业网跨地域带宽默认为1kbps，仅供测试使用。如果您需要业务正常运行，请购买带宽包并设置跨地域互通带宽。

|配置	|说明|
|---|---|
|带宽包|	选择已绑定至云企业网实例的带宽包。|
|互通地域	|选择需要互通的地域。|
|带宽|	根据业务需要输入跨地域互通带宽的带宽值。每个带宽包下的跨地域互通带宽的总和不能大于该带宽包的带宽值。|