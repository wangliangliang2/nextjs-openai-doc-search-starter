# 删除签名 (DELETE Signature)

## 请求定义

```
DELETE /v1/signature/:id HTTP/1.1
Host: sms.qiniuapi.com
Authorization: <QiniuToken>
```

### 请求头

> 请参考 [请求认证](/sms/api/5842/sms-api-authentication)


## 响应定义

### 该接口正确处理请求时返回 HTTP 状态码

- 服务器端正确处理请求时响应 HTTP 状态码为 200