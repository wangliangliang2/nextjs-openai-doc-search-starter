七牛云支持服务端加密功能以提供静态数据保护。当 Kodo 对象存储在数据写入磁盘之前，支持在对象级别上应用数据加密的保护策略，并将得到的加密数据持久化保存下来。用户访问数据时，在拥有对象访问权限的前提下，自动对保存的加密数据进行解密并把原始数据返回。

Kodo 还提供针对特殊文件类型安全访问的功能，支持对指定文件类型可预览或下载、只下载无法预览以及禁止访问。用户可以按照文件类型区分敏感数据并进行对应设定。

上述功能服务，如有需要请[联系我们](https://support.qiniu.com/tickets/new/)咨询详情。