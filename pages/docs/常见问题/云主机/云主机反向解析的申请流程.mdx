qvm 反向解析需要提交工单由售后工程师操作，提交工单时请注明 主机所在区域（北京、上海等）和 ip 对应的解析域名。

 
FAQ:

Q: 1个IP地址是否允许指向多个反解域名 ?

A:  一个IP只允许对应一个反解域名


Q: 设置提交后，如何测试是否生效？

A: 提交后2分钟后会在权威DNS上生效, 可以使用 dig 命令查看

`
dig -x 47.254.32.63 +short
gw8.4cun.com.

`



