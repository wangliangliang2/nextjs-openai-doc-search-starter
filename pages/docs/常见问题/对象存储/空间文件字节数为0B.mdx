表现：&nbsp;  
加载图片时显示成功，但是显示为空白；空间中的文件大小为 0B

原因：&nbsp;  
1. 七牛允许 0B 字节的文件存在。&nbsp;  
2. 客户端上传时读取文件错误。

解决方法：&nbsp;  
1. 更新使用的客户端sdk，最新的sdk中已经禁止 0B 的文件上传。&nbsp;  
2. 在上传策略中设置 fsizeMin 为1，保证上传的文件大于 1B 。

