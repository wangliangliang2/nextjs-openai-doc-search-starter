> 问题描述： 为什么查询的时候不能按我自己日志里的时间搜索？

默认情况下，logkit 不会自动识别您日志里的时间字段，您需要在数据转换部分选择 `date` 转换器，勾选你要转换的时间字段。（注意，智能日志管理平台是不能修改数据类型的，所以如果你这个字段之前已经确定为 string 类型，此时要设置为时间字段，就要再进行一次重命名，可以使用`rename`转换器）

> 问题描述： logkit-pro  里面提供 ip 解析，但是我没有 ip 库怎么办？

如果客户没有 ip 库，可以配置服务端解析 ip ，这个是七牛使用自己购买的商业 ip 库帮您转换，也是免费的。