## 升级说明

七牛连麦服务从 v1 版本迁移至 v2 版本时，直接将相关接口 path 中的 v1 前缀改为 v2 前缀即可。

v2 版本连麦是以 “即时使用，即时申请”的方式管理房间的，所以不需要客户主动请求服务端接口调用 CreateRoom 和 DeleteRoom。考虑到客户已经将“创建/删除”房间的动作集成到自己的业务处理流程，v2 版本连麦暂时保留了相关接口的伪实现（永远返回 200），客户可以在不变更业务处理流程的情况下进行升级。